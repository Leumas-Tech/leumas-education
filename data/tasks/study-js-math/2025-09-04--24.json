{
  "id": "i9u161hh1xl",
  "date": "2025-09-04",
  "practice": "study-js-math",
  "title": "Study — JavaScript + Mathematics — 2025-09-04",
  "brief": "Warm up on today’s topic. Read the brief, then solve the 2-minute task.",
  "exercise": {
    "title": "Two-minute arrays task",
    "instructions": "Write a function sum(arr) that returns the sum of numbers; [] -> 0.",
    "starterCode": "function sum(arr){\n  // your code\n}\nconsole.log(sum([1,2,3])); // 6",
    "answerKey": "Use reduce or loop; return 0 for empty array."
  },
  "steps": [
    {
      "label": "Read the brief"
    },
    {
      "label": "Do the 2-minute task"
    }
  ],
  "quiz": null,
  "acceptance": {
    "type": "problem",
    "minScore": 0.7,
    "kind": "code",
    "prompt": "Write a function sum(arr) that returns the sum of numbers; [] -> 0."
  },
  "status": "failed",
  "score": 0,
  "attempts": [
    {
      "ts": 1757042475056,
      "type": "problem",
      "payload": {
        "answer": "function sum (arr) { \n\n}\n"
      },
      "auto": {
        "score": 0.4,
        "feedback": "Missing implementation to calculate the sum of numbers in the array.",
        "solution": "function sum(arr) { if (arr.length === 0) { return 0; } else { let total = arr[0]; for (let i = 1; i < arr.length; i++) { total += arr[i]; } return total; }"
      },
      "passed": false
    }
  ],
  "solution": "function sum(arr) { if (arr.length === 0) { return 0; } else { let total = arr[0]; for (let i = 1; i < arr.length; i++) { total += arr[i]; } return total; }"
}